#include <vector>
#include <unordered_map>
#include <map>
#include <string>
#include "DataStructures.hpp"

void mainAllocation(std::map<std::string, std::map<std::string, std::unordered_map<int, bool>>> &availibility_data, std::vector<Course> courses, int margin){

    /* The preprocessing of the data*/
    
    /* The logic of allocating lecture hall first*/

    /* The logic for allocating tutorials*/
    return;
}