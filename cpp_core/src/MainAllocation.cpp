#include<bits/stdc++.h>

void mainAllocation(map<string, map<string, unordered_map<int, bool>>> &availibility_data, vector<Course> courses, int margin){4

    /* The preprocessing of the data*/
    
    /* The logic of allocating lecture hall first*/

    /* The logic for allocating tutorials*/
    return;
}